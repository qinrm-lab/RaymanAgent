# Rayman managed workflow: generated by ./.Rayman/scripts/project/generate_project_workflows.ps1
name: rayman-project-fast-gate

on:
  pull_request:
    paths:
{{FAST_PATHS}}
  push:
    branches:
      - main
      - master
    paths:
{{FAST_PATHS}}
  workflow_dispatch:

jobs:
  fast-gate-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Rayman fast gate dependencies
        shell: bash
        run: |
          python3 -m pip install --upgrade pip jsonschema

      - name: Run Rayman fast gate
        shell: pwsh
        run: |
          ./.Rayman/rayman.ps1 fast-gate

      - name: Upload Rayman fast gate artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: {{ARTIFACT_PREFIX}}-fast-gate-linux
          path: |
            .Rayman/runtime/project_gates/**
            .Rayman/runtime/*.json
            .Rayman/logs/**
{{WINDOWS_FAST_JOB}}
